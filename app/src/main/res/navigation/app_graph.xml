<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_graph"
    app:startDestination="@id/weather_details">

    <fragment
        android:id="@+id/weather_details"
        android:name="sashjakk.weather.app.ui.WeatherDetailsFragment"
        tools:layout="@layout/fragment_weather_details">

        <action
            android:id="@+id/details_to_list"
            app:destination="@id/weather_list" />
    </fragment>

    <fragment
        android:id="@+id/weather_list"
        android:name="sashjakk.weather.app.ui.WeatherListFragment" />

</navigation>